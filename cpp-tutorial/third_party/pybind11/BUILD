load("@bazel_skylib//rules:build_test.bzl", "build_test")

build_test(
    name = "build_test",
    targets = [
        # "@com_github_jupp0r_prometheus_cpp//pull",
        "@pybind11//:push",
    ],
    visibility = ["//:__pkg__"],
)


build_test(
    name = "build_test",
    targets = [
        "@uuid//:uuid",
    ],
)
