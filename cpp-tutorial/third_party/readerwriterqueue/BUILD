load("@bazel_skylib//rules:build_test.bzl", "build_test")

build_test(
    name = "build_test",
    targets = [
        "@readerwriterqueue//:readerwriterqueue",
    ],
    visibility = ["//:__pkg__"],
)
