#include <iostream>
#include <vector>

using namespace std;
vector<int> printMatrix(vector<vector<int>> matrix)
{
    
}
int main()
{
    int M, N;
    // cin >> "The rows of matrix" >> M >> "The columns of matrix" >> N;
    cin >> M >> N;
    cout << M << N << endl;
    return 0;
}