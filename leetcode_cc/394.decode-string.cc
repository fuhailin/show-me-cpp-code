#include <stack>
#include <string>
using namespace std;

class Solution {
 public:
  string decodeString(string s) {
    stack<string> stk;
    string res;
    int multi = 0;
    int i = 0;
    while (i < s.size()) {
        char c = s[i];
        if(c == '['){
            string 
        }
    }

  }
};