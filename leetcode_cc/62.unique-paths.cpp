#include <iostream>
#include <vector>

using namespace std;

int uniquePaths(int m, int n)
{
    vector<vector<int>> vec(m, vector<int>(n, 1));
    for(int )
}

int main(int argc, char const *argv[])
{

    return 0;
}