#include "bits/stdc++.h"

class Solution {
public:
    string decodeCiphertext(string encodedText, int rows) {
        
    }
};

int main(int argc, char const *argv[])
{
    Solution so;
    string originalText = "cipher";
    string encodedText = so.decodeCiphertext(originalText);
    cout << "encodedText: " << encodedText << endl;
    return 0;
}
